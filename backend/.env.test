# Test overrides — loaded AFTER .env (with override=True) by conftest.py.
# When running pytest on the host machine, Docker maps postgres:5432 → localhost:5432.
# Inside a Docker container, set POSTGRES_HOST=postgres before running pytest instead.
POSTGRES_HOST=localhost
